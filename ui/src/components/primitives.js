export const escapeHtml = (value) => String(value).replace(/[&<>'"]/g, (c) => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))
export const badge = (text, tone='neutral') => `<span class="badge badge-${tone}">${escapeHtml(text)}</span>`
export const metric = (label, value, detail='', metadata={}) => {const meta={definition:metadata.definition??label,units:metadata.units??'displayed',timeBasis:metadata.timeBasis??'current projection',source:metadata.source??'policy-filtered read model',quality:metadata.quality??'fixture-qualified',freshness:metadata.freshness??'sequence bounded'};return `<article class="metric" data-metric-source="${escapeHtml(meta.source)}"><p>${escapeHtml(label)}</p><strong>${escapeHtml(value)}</strong>${detail ? `<small>${escapeHtml(detail)}</small>` : ''}<details class="metric-metadata"><summary>Metric definition</summary><dl>${Object.entries(meta).map(([key,item])=>`<div><dt>${escapeHtml(key)}</dt><dd>${escapeHtml(item)}</dd></div>`).join('')}</dl></details></article>`}
export const panel = (title, body, kicker='') => `<section class="panel">${kicker ? `<p class="kicker">${escapeHtml(kicker)}</p>`:''}<h2>${escapeHtml(title)}</h2>${body}</section>`
