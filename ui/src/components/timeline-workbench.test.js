import{describe,expect,it}from'vitest';import{timelineWorkbench}from'./timeline-workbench.js';import{createTimelineState}from'../state/timeline.js';describe('timeline workbench',()=>{it('has accessible chart, table, controls and gap',()=>{const t=createTimelineState({source:'replay'});t.connect();t.ingest([{sequence:1,eventTimeMs:10,value:2,quality:.8,series:'signal'}],{gap:{dropped:4}});document.body.innerHTML=timelineWorkbench(t.getState(),t.visible());expect(document.querySelector('svg title').textContent).toContain('replay');expect(document.querySelector('table caption').textContent).toContain('dropped');expect(document.querySelector('[role=alert]').textContent).toContain('4 samples');expect(document.querySelector('[data-replay=play]').disabled).toBe(false)});it('escapes series and renders empty disconnect',()=>{const t=createTimelineState();t.ingest([{sequence:1,eventTimeMs:1,value:1,quality:1,series:'<img src=x>'}]);expect(timelineWorkbench(t.getState(),t.visible())).not.toContain('<img src=x>');const e=createTimelineState();document.body.innerHTML=timelineWorkbench(e.getState(),[]);expect(document.querySelector('[aria-live]').textContent).toBe('Disconnected');expect(document.querySelector('tbody').textContent).toContain('No authorized')})});
